language: python
python:
  - "3.6"

services:
  - docker

install:
  - pip install -r requirements.txt

before_script:
  - bash create.sh

script:
  - python test.py

after_script:
  - bash destroy.sh
